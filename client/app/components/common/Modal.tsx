import { ObjectData } from "../helper/types";

export default function Modal({
  show,
  fields,
  handleClose,
}: {
  show: boolean;
  fields: ObjectData;
  handleClose: () => void;
}) {
  return <></>;
}
